{{!< admin}}
<title>Báo Cáo - Thống Kê</title>

<div class="dashboard-header">
    <h3 class="dashboard-title mb-0 text-2xl">Báo Cáo - Thống Kê</h3>
    <button class="btn btn-success">
        <i class="fas fa-download"></i> Xuất báo cáo
    </button>
</div>

<main class="container">
<div class="space-y-8 p-6 bg-gray-50 min-h-screen text-gray-800">

    {{#if error}}
        <div class="alert alert-danger">{{error}}</div>
    {{/if}}

    <!-- ===== THỐNG KÊ TỔNG QUAN ===== -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">

        <div class="bg-white p-4 rounded-xl shadow-sm border flex justify-between items-center">
            <div>
                <p class="text-2xl font-bold">{{totalUsers}}</p>
                <p class="text-sm text-gray-500 mt-1">Tổng học viên</p>
            </div>
            <div class="bg-blue-100 p-2 rounded-lg">
                <i class="fas fa-users text-blue-600"></i>
            </div>
        </div>

        <div class="bg-white p-4 rounded-xl shadow-sm border flex justify-between items-center">
            <div>
                <p class="text-2xl font-bold">{{totalVideos}}</p>
                <p class="text-sm text-gray-500 mt-1">Tổng video</p>
            </div>
            <div class="bg-green-100 p-2 rounded-lg">
                <i class="fas fa-book text-green-600"></i>
            </div>
        </div>

        <div class="bg-white p-4 rounded-xl shadow-sm border flex justify-between items-center">
            <div>
                <p class="text-2xl font-bold">{{totalRegisters}}</p>
                <p class="text-sm text-gray-500 mt-1">Lượt đăng ký</p>
            </div>
            <div class="bg-purple-100 p-2 rounded-lg">
                <i class="fas fa-chart-line text-purple-600"></i>
            </div>
        </div>

        <div class="bg-white p-4 rounded-xl shadow-sm border flex justify-between items-center">
            <div>
                <p class="text-2xl font-bold">{{completedVideos}}</p>
                <p class="text-sm text-gray-500 mt-1">Hoàn thành</p>
            </div>
            <div class="bg-amber-100 p-2 rounded-lg">
                <i class="fas fa-trophy text-amber-600"></i>
            </div>
        </div>

    </div>

    <!-- ===== XU HƯỚNG ĐĂNG KÝ (T0 → T5) ===== -->
    <div class="bg-white rounded-xl p-6 shadow-sm border">
        <h2 class="text-lg font-semibold text-gray-900 mb-4">Xu hướng đăng ký (12 tháng)</h2>

        <div class="space-y-3">
        {{#each growthMonths}}
            <div class="flex items-center justify-between">
                <span class="w-10 text-sm font-medium">{{label}}</span>
                <div class="flex-1 h-2 bg-gray-200 rounded-full mx-2">
                    <div class="h-2 bg-blue-500 rounded-full" style="width: {{value}}%"></div>
                </div>
                <span class="text-sm text-gray-600">{{value}}</span>
            </div>
        {{/each}}
        </div>
    </div>

    <!-- ===== TIẾN ĐỘ CHUNG ===== -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">

        <!-- Tiến độ -->
        <div class="bg-white rounded-xl p-6 shadow-sm border text-center">
            <div class="w-24 h-24 mx-auto bg-blue-50 rounded-full flex items-center justify-center text-3xl font-bold text-blue-600 mb-4">
                {{averageProgress}}%
            </div>
            <h3 class="font-medium text-gray-900">Tiến độ trung bình</h3>
            <p class="text-sm text-gray-600">Của tất cả học viên</p>
        </div>

        <!-- Chỉ số tương tác -->
        <div class="bg-white rounded-xl p-6 shadow-sm border space-y-4">
            <div class="flex justify-between">
                <span class="text-sm text-gray-600">🎯 Tỷ lệ hoàn thành video</span>
                <span class="font-bold text-gray-900">{{completedVideos}} video</span>
            </div>
            <div class="flex justify-between">
                <span class="text-sm text-gray-600">📚 Tổng video</span>
                <span class="font-bold text-gray-900">{{totalVideos}}</span>
            </div>
        </div>

    </div>

</div>
</main>
