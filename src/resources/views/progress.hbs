
    <style>
        .card {
            border-radius: 15px;
            box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s, box-shadow 0.3s;
            background: rgba(255, 255, 255, 0.9);
            color: black;
            position: relative;
            overflow: hidden;
        }
        .card:hover {
            transform: scale(1.07);
            box-shadow: 0px 15px 40px rgba(0, 0, 0, 0.4);
        }
        .dashboard-title {
            font-size: 28px;
            font-weight: bold;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
        }
        .icon-box {
            font-size: 40px;
            margin-bottom: 10px;
        }
        .chart-container {
            width: 50%;
            margin: auto;
        }
    </style>

            <main>
                <h2 class="dashboard-title mb-4">📊 Tiến Độ Học Tập</h2>
                <div class="card p-4 mb-4 chart-container">
                    <canvas id="progressChart"></canvas>
                </div>
                <table class="table table-light table-hover text-center">
                    <thead class="table-dark">
                        <tr>
                            <th>Khóa Học</th>
                            <th>Giảng Viên</th>
                            <th>Tiến Độ</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>Lập Trình Web</td>
                            <td>Nguyễn Văn A</td>
                            <td>75%</td>
                        </tr>
                        <tr>
                            <td>Cơ Sở Dữ Liệu</td>
                            <td>Trần Thị B</td>
                            <td>60%</td>
                        </tr>
                    </tbody>
                </table>
            </main>
        </div>
    </div>

    <script>
        const ctx = document.getElementById('progressChart').getContext('2d');
        new Chart(ctx, {
            type: 'bar',
            data: {
                labels: ['Lập Trình Web', 'Cơ Sở Dữ Liệu'],
                datasets: [{
                    label: 'Tiến Độ (%)',
                    data: [75, 60],
                    backgroundColor: ['#4CAF50', '#FF9800'],
                    borderColor: ['#388E3C', '#F57C00'],
                    borderWidth: 1
                }]
            },
            options: {
                scales: {
                    y: {
                        beginAtZero: true,
                        max: 100
                    }
                }
            }
        });
    </script>

